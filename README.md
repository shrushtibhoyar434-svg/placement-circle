# PlacementCircle

A simple, responsive landing page for a student career community.

Open `index.html` directly in a browser to preview it. The page uses remote Unsplash imagery and Google Fonts, so an internet connection improves the visual result.
